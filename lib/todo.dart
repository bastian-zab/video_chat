//TODOS: Remaining Hurdles
/*
 Failed to load providerinstaller module:
No acceptable module com.google.android.gms.providerinstaller.dynamite found.
Local version is 0 and remote version is 0.
[   +1 ms] W/ProviderInstaller( 1389): Failed to report request stats:
reportRequestStats [class android.content.Context, long, long]

/*
'Savage_Opress', name: ''

1. Firebase authetication and account 
2.Firebase Data
3.Download black theme
4. Video
5.git
6. KYC
7. Uncomment, build, test on Android.

...

2.theme should be from user provider
3. video
4.Show loading while loading...esp while heading to entry page







*/

   /* final ImagePicker picker = ImagePicker();

                    final XFile? image =
                        await picker.pickImage(source: ImageSource.gallery);
                    var bytes = await image?.readAsBytes();
                    print(bytes.toString());

                    if (bytes != null) {
                      print("Detected Data");
                    } else {
                      print("Error detecting data");
                    }
*/*/